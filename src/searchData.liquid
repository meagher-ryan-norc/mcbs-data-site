---
permalink: /js/searchData.js
---
const searchArr = [
  {% for post in collections.sortedCovid %}{
    "title":"{{post.data.title}}",
    "desc":"{{post.data.description}}",
    "keywords":"{{post.data.keywords}}",
    "url":"{{post.url}}"
  },
  {% endfor %}
  {% for post in collections.sortedPuf %}{
    "title":"{{post.data.title}}",
    "desc":"{{post.data.description}}",
    "keywords":"{{post.data.keywords}}",
    "url":"{{post.url}}"
  }{% unless forloop.last %},{% endunless %}
  {% endfor %}
]
